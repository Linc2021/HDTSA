% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FTS_wntest.R
\name{WN_FTS_test}
\alias{WN_FTS_test}
\title{Testing for functional white noise in high dimensions}
\usage{
WN_FTS_test(
  Y_arr,
  U = NULL,
  lag.k = 2,
  B = 1000,
  alpha = 0.05,
  kernel.type = c("QS", "Par", "Bart"),
  is.smooth = FALSE
)
}
\arguments{
\item{Y_arr}{A \eqn{n \times p \times M} array, where \eqn{n} is the number of subjects, 
\eqn{p} is the dimension of the functional variables, and \eqn{M} is the number of time points.}

\item{U}{A \eqn{n \times M} matrix of observation time points. Required if \code{is.smooth = TRUE}.}

\item{lag.k}{The maximum time lag \eqn{L} to be tested. Default is 2.}

\item{B}{The number of bootstrap replications. Default is 1000.}

\item{alpha}{The significance level. Default is 0.05.}

\item{kernel.type}{The symmetric kernel used for long-run covariance estimation. 
Options: \code{"QS"} (default), \code{"Par"}, \code{"Bart"}.}

\item{is.smooth}{Logical. If \code{TRUE}, performs local linear smoothing on \code{Y_arr}. 
If \code{FALSE}, uses \code{Y_arr} directly. Default is \code{FALSE}.}
}
\value{
A list of class \code{"hdtstest"}, which contains the following
  components:

  \item{statistic}{The test statistic of the test.}
  \item{p.value}{The p-value of the test.}
  \item{crit.value}{The critical value of the test at the significance level alpha.} 
  \item{reject}{Reject or not.}
  \item{lag.k}{The lag for the test.}
  \item{kernel.type}{The type of kernel for the test.}
  \item{is.smooth}{Smooth the original data or not.}
  \item{computation.method}{Use matrix version to compute or vector version.}
}
\description{
Implements the high-dimensional white noise test for functional time series. 
It supports optional local linear smoothing and automatically switches between 
matrix and vectorized C++ implementations based on data dimensions.
}
